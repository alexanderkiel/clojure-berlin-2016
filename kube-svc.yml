apiVersion: v1
kind: Service
metadata:
  name: clojure-berlin-2016
  labels:
    app: lens
spec:
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    service: clojure-berlin-2016
